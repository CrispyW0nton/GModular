"""
GModular core — module state, I/O service, and application event bus.

Submodules:
  module_state — ModuleState, ModuleProject, Command hierarchy (undo/redo)
  module_io    — ModuleIO service (stateless loader; extracted from ModuleState)
  events       — EventBus + stable event-name constants
"""
