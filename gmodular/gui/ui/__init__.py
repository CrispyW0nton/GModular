"""
GModular — Qt Designer .ui files package.

All .ui files in this directory were designed with Qt Designer and are
loaded at runtime via  gmodular.gui.ui_loader.load_ui().

Files
-----
inspector.ui          — Inspector / property panel (right sidebar)
twoda_editor.ui       — 2DA table editor panel
dlg_editor.ui         — DLG node-graph editor
mod_import_dialog.ui  — Module import (.mod/.erf/.rim) dialog
"""
